# LLM（LangChain）
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# 配置
pydantic>=2.0
pydantic-settings>=2.0

# TTS（Edge-TTS 免费、无需 key）
edge-tts>=6.1.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# 视频合成需系统安装 ffmpeg（主流程为 HTML + Remotion，无需 manim）

# 可选：FFmpeg 封装（也可用 subprocess 直接调 ffmpeg）
# ffmpeg-python>=0.2.0

# 测试
pytest>=8.0
